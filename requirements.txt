# Production dependencies only. Pinned to exact versions for reproducible
# builds — see requirements-dev.txt for test/lint/type-check tooling, which
# must never be installed into the production Docker image.
aiogram==3.29.1
SQLAlchemy==2.0.51
greenlet==3.5.3
aiosqlite==0.22.1
alembic==1.18.5
APScheduler==3.11.3
python-dotenv==1.2.2
pytz==2026.2
